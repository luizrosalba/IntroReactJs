import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
ReactDOM.render(<App />, document.getElementById("app")); 

/// olha ai o render passando o app 

/// sem o preset do babel teriamos que fazer um require para usar o es6
